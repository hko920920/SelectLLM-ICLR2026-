# Anonymous validation-closure artifact (V9 prose refresh)

This archive supports **Candidate Lists Are Priors: Registry-Refinement Attacks
on Active Model Selection**.

V9 changes no frozen numerical evidence, protocol, result array, or executable
validator from validated V8. It refreshes the anonymous manuscript after a bounded
novelty/threat-model clarification:

- public/acquired-reference T2 is identified as an exact-quality causal stress test,
  not hidden-label production admission;
- T1 and the natural exact alias are separated as the realism bridges;
- related work explicitly distinguishes static ranks, observed-match perturbations,
  tool-description attacks, static quotienting, and future label acquisition.

Validate offline byte integrity and anonymity with:

```text
python validate_step82_anonymous_release.py --integrity-only
```

Run the complete frozen result replay with network access for locked public
dependencies:

```text
python fetch_step68_public_dependencies.py
python validate_step82_anonymous_release.py --full
```

The historical filename `STEP82_RELEASE_MANIFEST.json` and validator command are
retained for replay compatibility. The manifest's schema is V3 and records the V8
base-manifest hash and the current manuscript hash.

