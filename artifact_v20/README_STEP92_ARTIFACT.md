# Anonymous Step 92 review artifact

> **Scope correction retained in V14:** Step 92 is a learned raw-input
> decoupled-view audit: confidence-coded response equality drives acquisition,
> while hard-label correctness drives posterior evidence and regret. It is not
> an equation-faithful single-similarity Select-LLM reproduction. Step 93 tests
> that stricter condition prospectively and retains a positive cumulative but
> null-terminal Banking77 result. The V13 ZIP remains an immutable historical
> release; see the Step 93 closure report and V14 README for the current claim.

This V13 release binds the 22-page anonymous paper to the inherited causal,
prospective, defense, cross-selector, and configuration-wrapper evidence and to
the preregistered learned-adapter bridge reported in Appendix H.

The new condition was frozen before any AG News row or outcome was read. Twelve
independently trained 100,484-parameter bottleneck classifiers form the clean
registry. Four selected 50,817-parameter contextual temperature adapters have
distinct learned tensors, accept raw text through a hash-pinned public encoder,
and preserve all 30,400 parent hard answers. They raise sealed terminal regret by
0.547 [0.502, 0.593] percentage points across 1,000 paired pools; active-minus-
fixed harm is identical and fixed-query effects are exactly zero. This is one
new task under an already audited selector, not live registry admission, a full
independently fine-tuned checkpoint, or a universal attack claim.

## Validation

From the extracted artifact root, the offline command

```powershell
python validate_step92_anonymous_release.py --integrity-only
```

checks every bundled byte, the inherited V12 manifest, the current PDF binding,
all 32 Step 92 learned-weight files, and anonymity without network access.

With Python, PyTorch, Transformers, safetensors, and LaTeX dependencies installed,
the full command

```powershell
python validate_step92_anonymous_release.py --full
```

fetches and SHA-verifies the public DistilBERT encoder, independently replays
2,000 active and 2,000 fixed-query Step 92 trajectories in 4,170 checks, tests
256 raw-input endpoint samples, reruns four numerical tie/group-order policies,
reconstructs the retained Step 90 result, and rebuilds the paper while enforcing
the nine-page main-text boundary. The AG News dataset itself is not needed for
confirmatory replay because the hash-bound input-only holdout and sealed outcome
packages are included.

## Evidence layout

- `STEP92_LEARNED_CONTEXTUAL_ADAPTER_PREREGISTRATION_2026-08-12.md` is the
  pre-outcome protocol.
- `STEP92_STAGEA_COMPLETE_SEARCH_LEDGER_2026-08-12.json` retains all 300 Stage A
  conditions; the calibration outputs and all 12 clean plus 20 attack weight
  files are bundled.
- `STEP92_CONFIRMATORY_EXECUTION_LOCK_2026-08-12.json` binds the frozen inputs
  before the confirmatory output files existed.
- `STEP92_LEARNED_ADAPTER_CONFIRMATORY_RAW_2026-08-12.npz` and the paired JSON
  result contain the sealed 1,000-pool confirmation.
- `STEP92_INDEPENDENT_VALIDATION_2026-08-12.json` and
  `STEP92_NUMERIC_TIE_ROBUSTNESS_2026-08-12.json` bind the independent replay and
  the disclosed post-confirmatory floating-tie robustness audit.

Public base-encoder weights and third-party repositories are not embedded. The
dependency fetcher pins the exact public revision and verifies the weight SHA-256
before full replay.
